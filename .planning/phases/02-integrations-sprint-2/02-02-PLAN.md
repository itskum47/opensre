---
phase: 02-integrations-sprint-2
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - backend/app/providers/github.py
  - backend/app/domain/incidents/pipeline.py
  - backend/tests/test_integrations.py
autonomous: true
requirements: ["INTG-04", "PIPE-01"]

must_haves:
  truths:
    - "GitHub connector fetches commits successfully for target time ranges"
    - "InvestigationPipeline collection stage successfully invokes all configured plugins and writes to EventStore"
  artifacts:
    - path: "backend/app/providers/github.py"
      provides: "GitHub commit retrieval adapter"
      contains: "class GitHubDataSource"
    - path: "backend/tests/test_integrations.py"
      provides: "Test suite verifying integrations and collect stage"
      contains: "def test_prometheus_loki_collection"
---

<objective>
Implement GitHub DataSource, wire all plugins into the InvestigationPipeline collection stage, and build a testing suite verifying integrations.

Purpose: Integrated collection stage and verification.
Output: github.py adapter, updated pipeline.py enqueuer, and test_integrations.py test file.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-integrations-sprint-2/02-CONTEXT.md
@.planning/phases/02-integrations-sprint-2/02-RESEARCH.md
@.planning/phases/02-integrations-sprint-2/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement GitHub commit retrieval data source</name>
  <files>backend/app/providers/github.py</files>
  <read_first>sdk/plugin.py, backend/app/config/settings.py</read_first>
  <action>Implement `GitHubDataSource` in `backend/app/providers/github.py` extending `DataSourcePlugin`.
It queries the GitHub commit API range endpoint using `httpx` async client with Bearer Authorization headers.</action>
  <verify>python -c "from backend.app.providers.github import GitHubDataSource; print(GitHubDataSource)" exits 0</verify>
  <acceptance_criteria>
    - backend/app/providers/github.py contains "class GitHubDataSource(DataSourcePlugin)"
  </acceptance_criteria>
  <done>GitHub commit retrieval plugin completed</done>
</task>

<task type="auto">
  <name>Task 2: Wire all data source plugins into InvestigationPipeline collection stage</name>
  <files>backend/app/domain/incidents/pipeline.py</files>
  <read_first>backend/app/domain/incidents/pipeline.py, sdk/plugin.py</read_first>
  <action>Update the `collect` method in `backend/app/domain/incidents/pipeline.py`. It should:
- Instantiate list of datasource plugins: `PrometheusDataSource`, `LokiDataSource`, `KubernetesDataSource`, `GitHubDataSource`.
- Invoke `collect_evidence` for each plugin asynchronously.
- Call `EventStore.write_event` to persist normalized evidence packages to the EventStore directory.</action>
  <verify>python -c "from backend.app.domain.incidents.pipeline import InvestigationPipeline; print(InvestigationPipeline.collect)" exits 0</verify>
  <acceptance_criteria>
    - backend/app/domain/incidents/pipeline.py contains imports of all four provider adapters.
    - `collect` invokes and writes files to the EventStore.
  </acceptance_criteria>
  <done>Pipeline enqueued for evidence collection and store persistence</done>
</task>

<task type="auto">
  <name>Task 3: Implement unit and integration tests for all connectors</name>
  <files>backend/tests/test_integrations.py</files>
  <read_first>backend/app/domain/incidents/pipeline.py</read_first>
  <action>Create a test suite in `backend/tests/test_integrations.py` which:
- Verifies that all adapters inherit from `DataSourcePlugin`.
- Mocks HTTP REST calls to Prometheus, Loki, GitHub, and mock K8s client methods.
- Asserts that invoking the collect stage writes raw files containing payload details into the EventStore.</action>
  <verify>PYTHONPATH=. .venv/bin/pytest backend/tests/test_integrations.py</verify>
  <acceptance_criteria>
    - backend/tests/test_integrations.py passes test validations.
  </acceptance_criteria>
  <done>pytest test suite for integrations completed and verified</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Pytest runs all test suite validations in `test_integrations.py` successfully.
</verification>

<success_criteria>
- All tasks completed
- Integrations test suite verifies that K8s, metrics, logs, and git commits parse correctly
</success_criteria>

<output>
After completion, create `.planning/phases/02-integrations-sprint-2/02-02-SUMMARY.md`
</output>
