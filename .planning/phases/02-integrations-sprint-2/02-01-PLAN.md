---
phase: 02-integrations-sprint-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sdk/plugin.py
  - backend/app/providers/prometheus.py
  - backend/app/providers/loki.py
  - backend/app/providers/kubernetes.py
autonomous: true
requirements: ["SDK-01", "INTG-01", "INTG-02", "INTG-03"]

must_haves:
  truths:
    - "Prometheus and Loki query methods return correctly formatted event streams"
    - "Kubernetes adapter can authenticate and list cluster deployments, pods, and events"
  artifacts:
    - path: "sdk/plugin.py"
      provides: "DataSourcePlugin abstraction"
      contains: "class DataSourcePlugin"
    - path: "backend/app/providers/prometheus.py"
      provides: "Prometheus query client adapter"
      contains: "class PrometheusDataSource"
    - path: "backend/app/providers/loki.py"
      provides: "Loki query client adapter"
      contains: "class LokiDataSource"
    - path: "backend/app/providers/kubernetes.py"
      provides: "Kubernetes client adapter"
      contains: "class KubernetesDataSource"
---

<objective>
Define DataSourcePlugin base class and implement Prometheus, Loki, and Kubernetes integration providers.

Purpose: Core data ingestion infrastructure.
Output: plugin.py SDK, prometheus.py, loki.py, and kubernetes.py provider adapters.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-integrations-sprint-2/02-CONTEXT.md
@.planning/phases/02-integrations-sprint-2/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define DataSourcePlugin interface class</name>
  <files>sdk/plugin.py</files>
  <read_first>.planning/phases/02-integrations-sprint-2/02-RESEARCH.md</read_first>
  <action>Create a clean Python abstract base class `DataSourcePlugin` in `sdk/plugin.py` defining:
- `get_source_type(self) -> str`
- `async def collect_evidence(self, investigation_id: str, query_params: dict) -> list[dict]`</action>
  <verify>python -c "from sdk.plugin import DataSourcePlugin; print(DataSourcePlugin)" exits 0</verify>
  <acceptance_criteria>
    - sdk/plugin.py contains "class DataSourcePlugin"
    - `collect_evidence` is defined as an abstractmethod
  </acceptance_criteria>
  <done>DataSourcePlugin interface created successfully</done>
</task>

<task type="auto">
  <name>Task 2: Implement Prometheus and Loki DataSource plugins</name>
  <files>backend/app/providers/prometheus.py, backend/app/providers/loki.py</files>
  <read_first>sdk/plugin.py, backend/app/config/settings.py</read_first>
  <action>Implement `PrometheusDataSource` and `LokiDataSource` extending `DataSourcePlugin`.
- `PrometheusDataSource` queries Prometheus HTTP APIs range query endpoints using async `httpx.AsyncClient`.
- `LokiDataSource` queries Loki API using LogQL range queries with the 1000-line cap limit.</action>
  <verify>python -c "from backend.app.providers.prometheus import PrometheusDataSource; from backend.app.providers.loki import LokiDataSource; print(PrometheusDataSource, LokiDataSource)" exits 0</verify>
  <acceptance_criteria>
    - backend/app/providers/prometheus.py contains "class PrometheusDataSource(DataSourcePlugin)"
    - backend/app/providers/loki.py contains "class LokiDataSource(DataSourcePlugin)"
  </acceptance_criteria>
  <done>Prometheus and Loki query adapters completed</done>
</task>

<task type="auto">
  <name>Task 3: Implement Kubernetes resource state collection adapter</name>
  <files>backend/app/providers/kubernetes.py</files>
  <read_first>sdk/plugin.py, backend/app/config/settings.py</read_first>
  <action>Implement `KubernetesDataSource` extending `DataSourcePlugin`.
It must:
- Load K8s configuration (incluster config, fallback to kubeconfig).
- Query namespaced pods, deployments, services, and cluster events.
- Return structured dictionary metadata.</action>
  <verify>python -c "from backend.app.providers.kubernetes import KubernetesDataSource; print(KubernetesDataSource)" exits 0</verify>
  <acceptance_criteria>
    - backend/app/providers/kubernetes.py contains "class KubernetesDataSource(DataSourcePlugin)"
  </acceptance_criteria>
  <done>Kubernetes resource collection adapter completed</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Pytest mock runs assert that query formats match Prometheus and Loki endpoints.
- [ ] K8s adapter handles missing config files gracefully without crashing.
</verification>

<success_criteria>
- All tasks completed
- Infrastructure datasource connectors successfully retrieve event/metric data formats
</success_criteria>

<output>
After completion, create `.planning/phases/02-integrations-sprint-2/02-01-SUMMARY.md`
</output>
