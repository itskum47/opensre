---
phase: 07-historical-incident-memory
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - backend/app/domain/incidents/memory.py
  - backend/app/domain/incidents/ranker.py
  - backend/app/domain/incidents/pipeline.py
autonomous: true
requirements: ["MEM-01", "MEM-02"]

must_haves:
  truths:
    - "IncidentMemoryEngine Jaccard calculation matches incident similarity correctly"
    - "RootCauseRanker retrieves similar historical incidents and updates hypothesis score"
    - "Investigation pipeline indexes incident report in memory on completion"
  artifacts:
    - path: "backend/app/domain/incidents/memory.py"
      provides: "Jaccard similarity search logic"
      contains: "def find_similar_incidents"
---

<objective>
Implement Jaccard similarity query matching in IncidentMemoryEngine, integrate it with RootCauseRanker scoring, and index completed pipeline runs automatically.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-historical-incident-memory/07-CONTEXT.md
@.planning/phases/07-historical-incident-memory/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Jaccard similarity search in IncidentMemoryEngine</name>
  <files>backend/app/domain/incidents/memory.py</files>
  <read_first>backend/app/domain/incidents/memory.py</read_first>
  <action>Add `find_similar_incidents(self, active_keys: list[str], active_alerts: list[str], active_tokens: list[str], threshold: float = 0.2) -> list[dict]` to `IncidentMemoryEngine`.
It must:
- Load all `HistoricalIncident` records.
- For each record, calculate Jaccard similarity of keys, alerts, and tokens separately, and combine them (e.g. average).
- Filter out matches below threshold.
- Return sorted list of matching dicts containing details and remediation_action.</action>
  <verify>python -c "from backend.app.domain.incidents.memory import IncidentMemoryEngine; print(IncidentMemoryEngine.find_similar_incidents)" exits 0</verify>
  <acceptance_criteria>
    - backend/app/domain/incidents/memory.py contains "find_similar_incidents"
    - Jaccard calculation filters matches based on threshold.
  </acceptance_criteria>
  <done>Jaccard similarity search method implemented in MemoryEngine</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Memory Engine recommendations into RootCauseRanker</name>
  <files>backend/app/domain/incidents/ranker.py</files>
  <read_first>backend/app/domain/incidents/ranker.py, backend/app/domain/incidents/memory.py</read_first>
  <action>Modify `backend/app/domain/incidents/ranker.py`. In `__init__` accept optional `memory_engine: IncidentMemoryEngine = None`.
In `rank_hypotheses`, for each candidate group:
- Retrieve similar incidents matching candidate keys.
- If match exists, set `change_history` metric to similarity score and add to confidence (up to 0.25 max).
- Return suggested remediation plans from similar incidents in the ranked hypotheses output.</action>
  <verify>python -c "from backend.app.domain.incidents.ranker import RootCauseRanker; print(RootCauseRanker)" exits 0</verify>
  <acceptance_criteria>
    - backend/app/domain/incidents/ranker.py accepts memory_engine in __init__.
    - `change_history` incorporates historical similarity score.
  </acceptance_criteria>
  <done>Memory engine similarity queries integrated inside RootCauseRanker scoring</done>
</task>

<task type="auto">
  <name>Task 3: Index incident reports in pipeline on completion</name>
  <files>backend/app/domain/incidents/pipeline.py</files>
  <read_first>backend/app/domain/incidents/pipeline.py</read_first>
  <action>Update `InvestigationPipeline` in `backend/app/domain/incidents/pipeline.py` at the end of the `report` stage. If the report was written successfully, instantiate `IncidentMemoryEngine` and invoke `index_incident` to save the completed run to historical database memory.</action>
  <verify>python -c "from backend.app.domain.incidents.pipeline import InvestigationPipeline; print(InvestigationPipeline)" exits 0</verify>
  <acceptance_criteria>
    - Pipeline report stage indexes completed incident in memory.
  </acceptance_criteria>
  <done>Pipeline execution configured to index completed incident report snapshots in memory</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Write unit tests verifying similarity scores match mathematical expectations.
- Write unit tests verifying RootCauseRanker boosts confidence scores appropriately and includes remediation suggestions.
- Verify pipeline execution registers reports in memory database.
</verification>

<success_criteria>
- All tasks completed.
- Similar incidents boost ranker confidence scores and suggest historical remediation plans offline.
</success_criteria>

<output>
After completion, create `.planning/phases/07-historical-incident-memory/07-02-SUMMARY.md`.
</output>
