---
phase: 07-historical-incident-memory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/domain/incidents/models.py
  - backend/app/domain/incidents/memory.py
autonomous: true
requirements: ["MEM-01"]

must_haves:
  truths:
    - "HistoricalIncident DB model exposes keys, alerts, and error_tokens fields"
    - "IncidentMemoryEngine can save historical records into SQLite database"
  artifacts:
    - path: "backend/app/domain/incidents/memory.py"
      provides: "Storage engine indexing historical incident profiles"
      contains: "class IncidentMemoryEngine"
---

<objective>
Implement SQLite database model for HistoricalIncident, create IncidentMemoryEngine class, and write serializing/persisting index methods.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-historical-incident-memory/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement HistoricalIncident model in models.py</name>
  <files>backend/app/domain/incidents/models.py</files>
  <read_first>backend/app/domain/incidents/models.py</read_first>
  <action>Modify `backend/app/domain/incidents/models.py`. Create `HistoricalIncident` SQLAlchemy model containing:
- `id` (String primary key)
- `investigation_id` (String foreign key, nullable=True)
- `keys` (String representation of JSON array)
- `alerts` (String representation of JSON array)
- `error_tokens` (String representation of JSON array)
- `remediation_action` (String representation of JSON dict)</action>
  <verify>python -c "from backend.app.domain.incidents.models import HistoricalIncident; h = HistoricalIncident(); print(h)" exits 0</verify>
  <acceptance_criteria>
    - backend/app/domain/incidents/models.py contains "class HistoricalIncident"
    - Defines fields: keys, alerts, error_tokens, remediation_action.
  </acceptance_criteria>
  <done>HistoricalIncident SQLAlchemy DB model implemented</done>
</task>

<task type="auto">
  <name>Task 2: Build IncidentMemoryEngine serialization and index methods</name>
  <files>backend/app/domain/incidents/memory.py</files>
  <read_first>backend/app/domain/incidents/models.py</read_first>
  <action>Create `backend/app/domain/incidents/memory.py`. Implement `IncidentMemoryEngine` with:
- `__init__(self, db: Session)` accepting a database session.
- `index_incident(self, investigation_id: str, report_data: dict) -> bool` that parses report fields (root cause keys, active alerts, error tokens extracted from timeline logs, and remediation) and saves it as a `HistoricalIncident` record.</action>
  <verify>python -c "from backend.app.domain.incidents.memory import IncidentMemoryEngine; print(IncidentMemoryEngine)" exits 0</verify>
  <acceptance_criteria>
    - backend/app/domain/incidents/memory.py contains "class IncidentMemoryEngine"
    - Contains index_incident method parsing and writing records.
  </acceptance_criteria>
  <done>IncidentMemoryEngine serialization and index methods implemented</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Write unit tests validating HistoricalIncident ORM operations.
- Write unit tests verifying IncidentMemoryEngine indexes mock incident reports correctly.
- Run pytest suite.
</verification>

<success_criteria>
- All tasks completed.
- Database records for historical incidents can be written and verified cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/07-historical-incident-memory/07-01-SUMMARY.md`.
</output>
