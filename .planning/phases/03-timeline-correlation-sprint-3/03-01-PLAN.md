---
phase: 03-timeline-correlation-sprint-3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/domain/incidents/timeline.py
  - backend/app/domain/incidents/pipeline.py
  - backend/tests/test_timeline.py
autonomous: true
must_haves:
  truths:
    - "TimelineBuilder correctly aggregates and chronologically sorts Prometheus, Loki, Kubernetes, and GitHub events"
    - "CorrelationEngine links events within a 5-minute sliding window that share service/pod tags"
  artifacts:
    - path: "backend/app/domain/incidents/timeline.py"
      provides: "TimelineBuilder and CorrelationEngine classes"
      contains: "class TimelineBuilder"
    - path: "backend/tests/test_timeline.py"
      provides: "Unit tests for timeline normalization and correlation"
      contains: "def test_timeline_compilation"
---

# Phase 3 Plan 01: Build Timeline Builder and Correlation Engine

## Objective
- **What:** Build the core incident timeline compiler and entity correlation engine.
- **Why:** Normalizes raw data packages collected from all sources, sequences them chronologically, and groups correlated events together to feed the graph dependency engine.
- **Output:** timeline.py compiler, updated pipeline.py steps, and test_timeline.py tests.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-timeline-correlation-sprint-3/03-CONTEXT.md

## Tasks

<task type="auto">
  <name>Task 1: Implement TimelineBuilder and CorrelationEngine</name>
  <files>backend/app/domain/incidents/timeline.py</files>
  <read_first>backend/app/events/store.py, backend/app/providers/prometheus.py</read_first>
  <action>Create `backend/app/domain/incidents/timeline.py` containing:
- `class TimelineBuilder`: Reads raw events from `EventStore` for an investigation ID, normalizes them into unified dictionary models, and returns a list sorted by timestamp.
- `class CorrelationEngine`: Scans the normalized timeline events and groups them using a 5-minute sliding window and resource tag matching (pod, app, namespace).</action>
  <verify>python -c "from backend.app.domain.incidents.timeline import TimelineBuilder, CorrelationEngine; print(TimelineBuilder, CorrelationEngine)" exits 0</verify>
  <acceptance_criteria>
    - backend/app/domain/incidents/timeline.py contains "class TimelineBuilder"
    - backend/app/domain/incidents/timeline.py contains "class CorrelationEngine"
  </acceptance_criteria>
  <done>TimelineBuilder and CorrelationEngine implemented successfully</done>
</task>

<task type="auto">
  <name>Task 2: Wire timeline compiler stages in InvestigationPipeline</name>
  <files>backend/app/domain/incidents/pipeline.py</files>
  <read_first>backend/app/domain/incidents/pipeline.py, backend/app/domain/incidents/timeline.py</read_first>
  <action>Update `normalize`, `correlate`, and `timeline` methods in `backend/app/domain/incidents/pipeline.py` to:
- Instaniate `TimelineBuilder` and compile the raw evidence from `EventStore`.
- Run `CorrelationEngine` to group correlated events.
- Write the final sorted timeline and correlation groups as JSON files (e.g. `timeline.json` and `correlated_groups.json`) under the investigation events store directory.</action>
  <verify>python -c "from backend.app.domain.incidents.pipeline import InvestigationPipeline; print(InvestigationPipeline.normalize)" exits 0</verify>
  <acceptance_criteria>
    - backend/app/domain/incidents/pipeline.py imports `TimelineBuilder` and `CorrelationEngine`
    - `normalize`, `correlate`, and `timeline` methods contain logic to build and persist timeline files
  </acceptance_criteria>
  <done>InvestigationPipeline steps completed</done>
</task>

<task type="auto">
  <name>Task 3: Write tests for timeline compiler and correlation logic</name>
  <files>backend/tests/test_timeline.py</files>
  <read_first>backend/app/domain/incidents/timeline.py, backend/app/domain/incidents/pipeline.py</read_first>
  <action>Create a test suite in `backend/tests/test_timeline.py` asserting:
- Correct sorting order of events from different sources.
- Correct grouping of events sharing 'auth-service' tag within 5 minutes.
- Correct pipeline execution of stages 1 to 4.</action>
  <verify>PYTHONPATH=. .venv/bin/pytest backend/tests/test_timeline.py</verify>
  <acceptance_criteria>
    - backend/tests/test_timeline.py passes successfully
  </acceptance_criteria>
  <done>Timeline test suite completed and verified</done>
</task>

## Verification
- Run `PYTHONPATH=. .venv/bin/pytest backend/tests/test_timeline.py` to verify all timeline logic passes.

## Success Criteria
- [ ] TimelineBuilder correctly normalizes and sorts all raw event types.
- [ ] CorrelationEngine successfully groups co-occurring errors and spikes within 5 minutes.
