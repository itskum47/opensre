---
phase: 05-neo4j-storage-layer
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - backend/app/domain/incidents/graph.py
  - backend/app/domain/incidents/pipeline.py
autonomous: true
requirements: ["NEO4-01", "NEO4-02"]

must_haves:
  truths:
    - "GraphProvider integrates Neo4jConnector and delegates sync_node/sync_edge calls"
    - "Pipeline saves graph to Neo4j database successfully on execution"
    - "GraphProvider supports querying shared dependency paths from Neo4j"
  artifacts:
    - path: "backend/app/domain/incidents/graph.py"
      provides: "GraphProvider wrapping Neo4j persistence and path query features"
      contains: "class GraphProvider"
---

<objective>
Integrate Neo4jConnector within GraphProvider, trigger graph synchronization during pipeline runs, and implement the cross-incident shared dependency path querying.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-neo4j-storage-layer/05-CONTEXT.md
@.planning/phases/05-neo4j-storage-layer/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate Neo4jConnector in GraphProvider</name>
  <files>backend/app/domain/incidents/graph.py</files>
  <read_first>backend/app/domain/incidents/graph.py, backend/app/providers/graph/neo4j.py</read_first>
  <action>Modify `backend/app/domain/incidents/graph.py`. Initialize `Neo4jConnector` inside `GraphProvider.__init__`. Call `connect()` to establish connection. Update `add_node` and `add_edge` to call the connector's `sync_node` and `sync_edge` methods (providing a default investigation_id if not present or passed in). Make sure methods handle investigation_id parameters.</action>
  <verify>python -c "from backend.app.domain.incidents.graph import GraphProvider; g = GraphProvider(); print(g)" exits 0</verify>
  <acceptance_criteria>
    - backend/app/domain/incidents/graph.py imports "Neo4jConnector"
    - `add_node` and `add_edge` invoke Neo4jConnector sync methods.
  </acceptance_criteria>
  <done>Neo4jConnector integrated inside GraphProvider</done>
</task>

<task type="auto">
  <name>Task 2: Update Pipeline Execution to pass Investigation ID and Sync Graph</name>
  <files>backend/app/domain/incidents/pipeline.py</files>
  <read_first>backend/app/domain/incidents/pipeline.py, backend/app/domain/incidents/graph.py</read_first>
  <action>Update `InvestigationPipeline` in `backend/app/domain/incidents/pipeline.py` during the `graph` stage. Ensure `investigation_id` is passed when nodes and edges are added to `GraphProvider` (or update GraphProvider to accept/track investigation_id). Verify pipeline execution successfully triggers graph syncing.</action>
  <verify>python -c "from backend.app.domain.incidents.pipeline import InvestigationPipeline; print(InvestigationPipeline)" exits 0</verify>
  <acceptance_criteria>
    - Pipeline execution calls GraphProvider node/edge addition with active investigation ID.
  </acceptance_criteria>
  <done>Pipeline updated to propagate investigation ID to GraphProvider</done>
</task>

<task type="auto">
  <name>Task 3: Implement path tracing shared dependency Cypher queries</name>
  <files>backend/app/domain/incidents/graph.py</files>
  <read_first>backend/app/domain/incidents/graph.py</read_first>
  <action>Add a method `get_shared_dependencies(self, alert_names: list[str], max_hops: int = 3) -> list[str]` to `GraphProvider`. If Neo4j connector is active, it runs a Cypher query finding nodes that have directed paths from all target alert nodes within `max_hops` limits. If offline, fallback to NetworkX set intersections.</action>
  <verify>python -c "from backend.app.domain.incidents.graph import GraphProvider; g = GraphProvider(); print(g.get_shared_dependencies([]))" exits 0</verify>
  <acceptance_criteria>
    - backend/app/domain/incidents/graph.py contains "get_shared_dependencies"
    - Contains offline NetworkX fallback logic.
  </acceptance_criteria>
  <done>Path tracing shared dependency Cypher queries and NetworkX fallback implemented</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Write unit tests verifying `add_node` and `add_edge` call neo4j connector.
- Write unit tests verifying `get_shared_dependencies` executes correct Cypher query and correct NetworkX intersection query when offline.
- Run pytest suite to ensure 100% success.
</verification>

<success_criteria>
- All tasks completed.
- Neo4j graph synchronization runs automatically in the pipeline, and cross-incident path query functions as expected with offline fallback.
</success_criteria>

<output>
After completion, create `.planning/phases/05-neo4j-storage-layer/05-02-SUMMARY.md`.
</output>
