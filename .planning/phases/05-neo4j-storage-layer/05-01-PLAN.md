---
phase: 05-neo4j-storage-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/config/settings.py
  - backend/app/providers/graph/neo4j.py
autonomous: true
requirements: ["NEO4-01"]

must_haves:
  truths:
    - "System can load Neo4j configuration settings from environment variables"
    - "Neo4jConnector driver can initialize a connection pool and gracefully close it"
    - "Neo4jConnector can write and sync nodes and edges to Neo4j with fallback logic if offline"
  artifacts:
    - path: "backend/app/providers/graph/neo4j.py"
      provides: "Neo4j database connection pool and CRUD operation interface"
      contains: "class Neo4jConnector"
---

<objective>
Implement Neo4j configurations in settings.py, set up the official neo4j Python driver connection pool, and build a Neo4jConnector to sync nodes/edges with offline fallback.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-neo4j-storage-layer/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Neo4j Settings</name>
  <files>backend/app/config/settings.py</files>
  <read_first>.planning/phases/05-neo4j-storage-layer/05-CONTEXT.md</read_first>
  <action>Add `NEO4J_URI`, `NEO4J_USER`, and `NEO4J_PASSWORD` environment configurations to the Settings class in `backend/app/config/settings.py` with default values (bolt://localhost:7687, neo4j, password).</action>
  <verify>python -c "from backend.app.config.settings import settings; print(settings.NEO4J_URI)" outputs bolt://localhost:7687</verify>
  <acceptance_criteria>
    - backend/app/config/settings.py contains "NEO4J_URI"
    - backend/app/config/settings.py contains "NEO4J_USER"
    - backend/app/config/settings.py contains "NEO4J_PASSWORD"
  </acceptance_criteria>
  <done>Neo4j setting properties configured in BaseSettings class</done>
</task>

<task type="auto">
  <name>Task 2: Implement Neo4jConnector with driver pool lifecycle</name>
  <files>backend/app/providers/graph/neo4j.py</files>
  <read_first>backend/app/config/settings.py</read_first>
  <action>Create `backend/app/providers/graph/neo4j.py`. Implement `Neo4jConnector` containing:
- `__init__` resolving driver options from `settings`.
- `connect` initializing the Neo4j `GraphDatabase.driver` instance pool.
- `close` gracefully closing the driver connection.
- Robust exception handling when driver cannot connect (Offline fallback logic).</action>
  <verify>python -c "from backend.app.providers.graph.neo4j import Neo4jConnector; n = Neo4jConnector(); print(n)" exits 0</verify>
  <acceptance_criteria>
    - backend/app/providers/graph/neo4j.py contains "class Neo4jConnector"
    - Contains driver connection initialization and cleanup methods.
  </acceptance_criteria>
  <done>Neo4jConnector driver interface and lifecycle methods defined</done>
</task>

<task type="auto">
  <name>Task 3: Implement node and edge synchronization methods</name>
  <files>backend/app/providers/graph/neo4j.py</files>
  <read_first>backend/app/providers/graph/neo4j.py</read_first>
  <action>Add `sync_node` and `sync_edge` methods to `Neo4jConnector`. They should execute write transactions using Cypher queries:
- `sync_node(investigation_id, name, node_type, metadata)` using MERGE query.
- `sync_edge(investigation_id, source, target, relationship, metadata)` using MERGE query.
All operations should catch Neo4j exceptions, log warnings, and fallback gracefully without raising exceptions to prevent pipeline failures.</action>
  <verify>python -c "from backend.app.providers.graph.neo4j import Neo4jConnector; n = Neo4jConnector(); n.sync_node('test', 'service-a', 'service', {})" runs without exceptions even if Neo4j is offline</verify>
  <acceptance_criteria>
    - backend/app/providers/graph/neo4j.py contains "sync_node"
    - backend/app/providers/graph/neo4j.py contains "sync_edge"
    - Handled Neo4j exceptions with fallback logging warnings.
  </acceptance_criteria>
  <done>Node and edge Cypher sync methods with fallback logic implemented</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Write unit tests validating settings loading.
- Write unit tests mocking Neo4j GraphDatabase driver calls to verify `sync_node` and `sync_edge` construct Cypher queries correctly.
- Verify tests catch connectivity errors and log warnings rather than crashing.
</verification>

<success_criteria>
- All tasks completed.
- Neo4j connection pool and schema sync operations execute cleanly with robust offline fallback logging.
</success_criteria>

<output>
After completion, create `.planning/phases/05-neo4j-storage-layer/05-01-SUMMARY.md`.
</output>
