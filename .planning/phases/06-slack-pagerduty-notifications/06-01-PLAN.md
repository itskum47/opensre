---
phase: 06-slack-pagerduty-notifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/config/settings.py
  - backend/app/providers/notifications/slack.py
  - backend/app/main.py
autonomous: true
requirements: ["NOTF-01", "NOTF-03"]

must_haves:
  truths:
    - "SlackProvider is initialized with tokens and can send channel messages"
    - "FastAPI webhook verifies signature authenticity using SLACK_SIGNING_SECRET"
    - "Webhook parses interactive blocks approval payload and processes callback"
  artifacts:
    - path: "backend/app/providers/notifications/slack.py"
      provides: "Slack message delivery client and block kit builders"
      contains: "class SlackProvider"
---

<objective>
Implement Slack WebClient connection configurations, Block Kit alert templates, FastAPI webhook actions route, and HMAC SHA256 request signature verification.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-slack-pagerduty-notifications/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Slack configurations to settings.py</name>
  <files>backend/app/config/settings.py</files>
  <read_first>.planning/phases/06-slack-pagerduty-notifications/06-CONTEXT.md</read_first>
  <action>Add `SLACK_BOT_TOKEN`, `SLACK_CHANNEL`, and `SLACK_SIGNING_SECRET` env config parameters to Settings class in `backend/app/config/settings.py` (with defaults mock-token, general, mock-secret).</action>
  <verify>python -c "from backend.app.config.settings import settings; print(settings.SLACK_CHANNEL)" outputs general</verify>
  <acceptance_criteria>
    - backend/app/config/settings.py contains "SLACK_BOT_TOKEN"
    - backend/app/config/settings.py contains "SLACK_SIGNING_SECRET"
  </acceptance_criteria>
  <done>Slack configuration settings variables added to BaseSettings</done>
</task>

<task type="auto">
  <name>Task 2: Build SlackProvider client with Block Kit alert formats</name>
  <files>backend/app/providers/notifications/slack.py</files>
  <read_first>backend/app/config/settings.py</read_first>
  <action>Create `backend/app/providers/notifications/slack.py`. Implement `SlackProvider` containing:
- `__init__` loading settings.
- `send_alert(text, blocks=None)` sending slack message to `SLACK_CHANNEL` using `slack_sdk.WebClient`.
- `build_investigation_blocks(investigation_id, status, details)` constructing premium Block Kit blocks with interactive Approve/Reject buttons for remediation scripts.
Ensure exceptions are caught and logged without raising.</action>
  <verify>python -c "from backend.app.providers.notifications.slack import SlackProvider; s = SlackProvider(); print(s)" exits 0</verify>
  <acceptance_criteria>
    - backend/app/providers/notifications/slack.py contains "class SlackProvider"
    - Contains send_alert and build_investigation_blocks methods.
  </acceptance_criteria>
  <done>SlackProvider message delivery client implemented</done>
</task>

<task type="auto">
  <name>Task 3: Build interactive Slack webhook callback route with signature verification</name>
  <files>backend/app/main.py</files>
  <read_first>backend/app/providers/notifications/slack.py</read_first>
  <action>Modify `backend/app/main.py`. Add route `/api/v1/slack/actions` accepting POST requests.
Implement an authorization helper `verify_slack_signature(request: Request)` that:
- Reads the raw request body.
- Obtains headers: `X-Slack-Signature` and `X-Slack-Request-Timestamp`.
- Computes SHA256 HMAC of `v0:timestamp:body` matching signature.
Expose actions handler checking the interactive block action payload and returning a 200 response.</action>
  <verify>python -c "from backend.app.main import app; print(app.routes)" exits 0</verify>
  <acceptance_criteria>
    - backend/app/main.py contains `/api/v1/slack/actions` endpoint.
    - Implemented HMAC signature verification check.
  </acceptance_criteria>
  <done>FastAPI Slack interactive webhook actions route implemented with signature middleware verification</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Write unit tests validating settings loading.
- Write unit tests validating SlackProvider mock calls and Block Kit payload construction.
- Write unit tests verifying `/api/v1/slack/actions` route correctly rejects invalid signatures with 401/403 and accepts valid signatures.
</verification>

<success_criteria>
- All tasks completed.
- Slack client can deliver Block Kit alerts, and signature verification rejects unauthorized requests.
</success_criteria>

<output>
After completion, create `.planning/phases/06-slack-pagerduty-notifications/06-01-SUMMARY.md`.
</output>
