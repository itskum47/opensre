---
phase: 06-slack-pagerduty-notifications
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - backend/app/config/settings.py
  - backend/app/providers/notifications/pagerduty.py
  - backend/app/providers/notifications/router.py
  - backend/app/domain/incidents/pipeline.py
autonomous: true
requirements: ["NOTF-02", "NOTF-04"]

must_haves:
  truths:
    - "PagerDutyProvider triggers, acknowledges, and resolves incidents via REST requests"
    - "PagerDutyProvider appends OpenSRE findings notes to PagerDuty incidents"
    - "NotificationRouter dispatches lifecycle transition events during pipeline execution"
  artifacts:
    - path: "backend/app/providers/notifications/pagerduty.py"
      provides: "PagerDuty client routing trigger/ack/resolve events"
      contains: "class PagerDutyProvider"
    - path: "backend/app/providers/notifications/router.py"
      provides: "Central NotificationRouter called from pipeline"
      contains: "class NotificationRouter"
---

<objective>
Implement PagerDuty client with trigger/ack/resolve REST calls and note sync, build central NotificationRouter, and wire it to active pipeline execution transitions.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-slack-pagerduty-notifications/06-CONTEXT.md
@.planning/phases/06-slack-pagerduty-notifications/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PagerDuty settings variables</name>
  <files>backend/app/config/settings.py</files>
  <read_first>backend/app/config/settings.py</read_first>
  <action>Add `PAGERDUTY_API_KEY` (default mock-key) and `PAGERDUTY_ROUTING_KEY` (default mock-key) parameters to BaseSettings in `backend/app/config/settings.py` if not already present.</action>
  <verify>python -c "from backend.app.config.settings import settings; print(settings.PAGERDUTY_API_KEY)" outputs mock-key</verify>
  <acceptance_criteria>
    - backend/app/config/settings.py contains "PAGERDUTY_API_KEY"
  </acceptance_criteria>
  <done>PagerDuty configurations added to settings.py</done>
</task>

<task type="auto">
  <name>Task 2: Build PagerDutyProvider client</name>
  <files>backend/app/providers/notifications/pagerduty.py</files>
  <read_first>backend/app/config/settings.py</read_first>
  <action>Create `backend/app/providers/notifications/pagerduty.py`. Implement `PagerDutyProvider` containing:
- `__init__` loading settings.
- `acknowledge_incident(incident_id)` and `resolve_incident(incident_id)` using PagerDuty API request calls (direct REST HTTP client calls using standard `urllib` or `requests` to PagerDuty endpoint `https://api.pagerduty.com/incidents/...` or via the official `pagerduty` library).
- `add_note(incident_id, content)` appending timeline notes to the incident.
Ensure exceptions are caught and logged without raising.</action>
  <verify>python -c "from backend.app.providers.notifications.pagerduty import PagerDutyProvider; p = PagerDutyProvider(); print(p)" exits 0</verify>
  <acceptance_criteria>
    - backend/app/providers/notifications/pagerduty.py contains "class PagerDutyProvider"
    - Contains acknowledge_incident, resolve_incident, and add_note methods.
  </acceptance_criteria>
  <done>PagerDutyProvider client implemented</done>
</task>

<task type="auto">
  <name>Task 3: Build NotificationRouter and wire to pipeline Celery stages</name>
  <files>backend/app/providers/notifications/router.py, backend/app/domain/incidents/pipeline.py</files>
  <read_first>backend/app/providers/notifications/slack.py, backend/app/providers/notifications/pagerduty.py</read_first>
  <action>Create `backend/app/providers/notifications/router.py`. Implement `NotificationRouter` importing `SlackProvider` and `PagerDutyProvider`. Add `dispatch(event_type, payload)` executing calls depending on feature flags.
Update `InvestigationPipeline` in `backend/app/domain/incidents/pipeline.py` to initialize `NotificationRouter` and dispatch alerts on start and complete events.</action>
  <verify>python -c "from backend.app.providers.notifications.router import NotificationRouter; print(NotificationRouter)" exits 0</verify>
  <acceptance_criteria>
    - backend/app/providers/notifications/router.py contains "class NotificationRouter"
    - backend/app/domain/incidents/pipeline.py invokes dispatch calls.
  </acceptance_criteria>
  <done>NotificationRouter created and hooked into pipeline execution transitions</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Write unit tests verifying PagerDutyProvider trigger/ack/resolve REST payloads.
- Write unit tests verifying NotificationRouter dispatches events based on settings feature flags.
- Run test suite to ensure all tests pass.
</verification>

<success_criteria>
- All tasks completed.
- Pipeline execution triggers Slack and PagerDuty notifications cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/06-slack-pagerduty-notifications/06-02-SUMMARY.md`.
</output>
