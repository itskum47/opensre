---
phase: 01-foundation-sprint-1
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - backend/app/events/store.py
  - backend/app/domain/audit/models.py
  - backend/app/domain/incidents/models.py
  - backend/app/domain/incidents/pipeline.py
  - backend/app/workers/tasks.py
autonomous: true
requirements: ["STOR-01", "STOR-02", "AUDT-01", "PIPE-01", "PIPE-02", "WORK-01", "WORK-02"]

must_haves:
  truths:
    - "Evidence is written immutably to backend/app/events/ and cannot be modified or deleted"
    - "API endpoints trigger async Celery tasks enqueued in Redis"
  artifacts:
    - path: "backend/app/events/store.py"
      provides: "Immutable file-based EventStore"
      contains: "class EventStore"
    - path: "backend/app/domain/audit/models.py"
      provides: "SQLAlchemy models for Audit Trail"
      contains: "class AuditRecord"
    - path: "backend/app/domain/incidents/pipeline.py"
      provides: "InvestigationPipeline manager and runner"
      contains: "class InvestigationPipeline"
    - path: "backend/app/workers/tasks.py"
      provides: "Asynchronous task queue enqueuers"
      contains: "def run_investigation"
---

<objective>
Implement Event Store, Audit Trail models, Investigation Pipeline runner, and asynchronous Worker queue.

Purpose: Core processing pipeline and asynchronous task execution engine.
Output: EventStore class, SQLAlchemy Audit and Incident tables, Pipeline coordinator, and Celery task tasks.py.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-sprint-1/01-CONTEXT.md
@.planning/phases/01-foundation-sprint-1/01-RESEARCH.md
@.planning/phases/01-foundation-sprint-1/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create immutable file-based Event Store</name>
  <files>backend/app/events/store.py</files>
  <read_first>.planning/phases/01-foundation-sprint-1/01-RESEARCH.md</read_first>
  <action>Implement `EventStore` in `backend/app/events/store.py`. It should:
- Maintain raw JSON files under `backend/app/events/{investigation_id}/`.
- Expose a method `write_event(investigation_id, source_type, source_id, payload)` that persists events.
- Never expose or allow delete/update operations on event files.</action>
  <verify>python -c "from backend.app.events.store import EventStore; print(EventStore)" exits 0</verify>
  <acceptance_criteria>
    - backend/app/events/store.py contains "class EventStore"
    - backend/app/events/store.py has no delete or update methods
  </acceptance_criteria>
  <done>EventStore created writing to file-system immutably</done>
</task>

<task type="auto">
  <name>Task 2: Define SQLAlchemy models for Audit Trail and Investigation Registry</name>
  <files>backend/app/domain/audit/models.py, backend/app/domain/incidents/models.py</files>
  <read_first>backend/app/config/settings.py</read_first>
  <action>Create database models using SQLAlchemy:
- `AuditRecord` containing: id, timestamp, action, investigation_id, payload. Block updates/deletes using SQLAlchemy event listeners (throw exceptions on modify).
- `Investigation` containing: id, status, started_at, completed_at, duration, pipeline_version, report_id.</action>
  <verify>python -c "from backend.app.domain.audit.models import AuditRecord; from backend.app.domain.incidents.models import Investigation; print(AuditRecord, Investigation)" exits 0</verify>
  <acceptance_criteria>
    - backend/app/domain/audit/models.py contains "class AuditRecord"
    - backend/app/domain/incidents/models.py contains "class Investigation"
  </acceptance_criteria>
  <done>Registry and Audit database tables configured with SQLAlchemy</done>
</task>

<task type="auto">
  <name>Task 3: Develop InvestigationPipeline coordinator and stages</name>
  <files>backend/app/domain/incidents/pipeline.py</files>
  <read_first>backend/app/domain/incidents/models.py, backend/app/events/store.py</read_first>
  <action>Implement `InvestigationPipeline` in `backend/app/domain/incidents/pipeline.py`. It must:
- Implement sequential execution of stages: `collect`, `normalize`, `correlate`, `timeline`, `graph`, `hypothesize`, `rank`, and `report`.
- Persist state changes in the `Investigation` database registry as it transitions from one stage to another.</action>
  <verify>python -c "from backend.app.domain.incidents.pipeline import InvestigationPipeline; print(InvestigationPipeline)" exits 0</verify>
  <acceptance_criteria>
    - backend/app/domain/incidents/pipeline.py contains "class InvestigationPipeline"
    - Contains stage methods or enum transition mappings matching collect, normalize, correlate, timeline, graph, hypothesize, rank, report.
  </acceptance_criteria>
  <done>InvestigationPipeline coordinator implemented with all stages mapped</done>
</task>

<task type="auto">
  <name>Task 4: Setup Celery task queue configurations and workers tasks</name>
  <files>backend/app/workers/tasks.py</files>
  <read_first>backend/app/domain/incidents/pipeline.py, backend/app/config/settings.py</read_first>
  <action>Configure Celery in `backend/app/workers/tasks.py`:
- Initialize `Celery("opensre")` using Redis URLs from Settings.
- Define `@celery.task` `run_investigation(investigation_id: str)` enqueuing pipeline execution.
- Ensure API router imports task delays.</action>
  <verify>python -c "from backend.app.workers.tasks import run_investigation; print(run_investigation)" exits 0</verify>
  <acceptance_criteria>
    - backend/app/workers/tasks.py contains "@celery.task" or equivalent task registration.
    - Contains "def run_investigation"
  </acceptance_criteria>
  <done>Celery task queue configured for async execution</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Pytest checks EventStore immutability (fails on delete/overwrite).
- [ ] Database listeners block updates and deletes to AuditRecord.
- [ ] Pipeline transitions match registry status values correctly.
</verification>

<success_criteria>
- All tasks completed
- Async worker enqueues and runs the pipeline stages correctly, updating registry status and audit trail
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-sprint-1/01-02-SUMMARY.md`
</output>
