---
phase: 01-foundation-sprint-1
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - backend/app/domain/incidents/schemas.py
  - backend/app/domain/incidents/snapshots.py
  - Dockerfile
  - docker-compose.yml
  - .github/workflows/ci.yml
  - .gitlab-ci.yml
  - backend/tests/test_config.py
  - backend/tests/test_llm.py
  - backend/tests/test_events.py
  - backend/tests/test_audit.py
  - backend/tests/test_pipeline.py
  - backend/tests/test_workers.py
  - backend/tests/test_snapshots.py
  - backend/tests/test_cicd.py
autonomous: true
requirements: ["SNAP-01", "SNAP-02", "REPT-01", "DOCK-01", "DOCK-02"]

must_haves:
  truths:
    - "Snapshots are structured JSON containing correct pipeline, ranking, graph, and prompt schema versions"
    - "Docker Compose validates successfully and links Celery, Redis, and FastAPI"
    - "CI workflows exist with tests and linters setup"
  artifacts:
    - path: "backend/app/domain/incidents/schemas.py"
      provides: "IncidentReportV1 schema definition"
      contains: "class IncidentReportV1"
    - path: "backend/app/domain/incidents/snapshots.py"
      provides: "Snapshot generation and loading logic"
      contains: "class SnapshotManager"
    - path: "docker-compose.yml"
      provides: "Docker orchestration config"
      contains: "celery"
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions CI workflow file"
      contains: "pytest"
    - path: ".gitlab-ci.yml"
      provides: "GitLab CI pipeline configuration"
      contains: "pytest"
---

<objective>
Build snapshot serializer, IncidentReportV1 schema, local Docker/Docker Compose configs, GitLab CI and GitHub Actions CI pipelines, and a complete pytest suite.

Purpose: Packaging, containerization, CI/CD setup, and automated unit testing framework.
Output: IncidentReportV1 Pydantic models, SnapshotManager, Dockerfile, docker-compose.yml, .github/workflows/ci.yml, .gitlab-ci.yml, and 8 pytest files.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-sprint-1/01-CONTEXT.md
@.planning/phases/01-foundation-sprint-1/01-RESEARCH.md
@.planning/phases/01-foundation-sprint-1/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define IncidentReportV1 schema and SnapshotManager</name>
  <files>backend/app/domain/incidents/schemas.py, backend/app/domain/incidents/snapshots.py</files>
  <read_first>backend/app/events/store.py</read_first>
  <action>Create Pydantic schema `IncidentReportV1` in `backend/app/domain/incidents/schemas.py` containing: schema_version, summary, root_cause, confidence, confidence_factors, timeline, evidence, blast_radius, remediation, similar_incidents, service_owners, metadata.
Create `SnapshotManager` in `backend/app/domain/incidents/snapshots.py` saving and loading snapshots under `backend/app/snapshots/` containing raw_events, timeline, graph, findings, report, and metadata (with schema, pipeline, graph, prompt, and ranking version properties).</action>
  <verify>python -c "from backend.app.domain.incidents.schemas import IncidentReportV1; from backend.app.domain.incidents.snapshots import SnapshotManager; print(IncidentReportV1, SnapshotManager)" exits 0</verify>
  <acceptance_criteria>
    - backend/app/domain/incidents/schemas.py contains "class IncidentReportV1"
    - backend/app/domain/incidents/snapshots.py contains "class SnapshotManager"
    - Snapshot manager output contains schema_version, pipeline_version, graph_version, prompt_version, model, temperature.
  </acceptance_criteria>
  <done>IncidentReportV1 schema and SnapshotManager written</done>
</task>

<task type="auto">
  <name>Task 2: Configure Dockerfile and docker-compose configurations</name>
  <files>Dockerfile, docker-compose.yml</files>
  <read_first>.planning/phases/01-foundation-sprint-1/01-RESEARCH.md</read_first>
  <action>Create a root-level `Dockerfile` and `docker-compose.yml`.
The `Dockerfile` builds a Python 3.12 environment, copies files, and installs dependencies.
The `docker-compose.yml` launches three containers:
- `api`: running fastapi
- `celery_worker`: running celery task worker
- `redis`: redis broker service</action>
  <verify>docker-compose config</verify>
  <acceptance_criteria>
    - docker-compose.yml contains services api, celery_worker, and redis.
    - docker-compose.yml sets broker port mapping or service dependencies.
  </acceptance_criteria>
  <done>Docker configurations created and verified</done>
</task>

<task type="auto">
  <name>Task 3: Configure GitLab CI and GitHub Actions CI pipelines</name>
  <files>.github/workflows/ci.yml, .gitlab-ci.yml</files>
  <read_first>.planning/phases/01-foundation-sprint-1/01-RESEARCH.md</read_first>
  <action>Write CI pipeline configuration files:
- `.github/workflows/ci.yml`: GitHub Actions setup triggering on pushes to master/main, installing python, running linter (Ruff) and pytest testing.
- `.gitlab-ci.yml`: GitLab CI workflow mirroring the GitHub steps.</action>
  <verify>test -f .github/workflows/ci.yml && test -f .gitlab-ci.yml</verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml contains "run: pytest" or similar
    - .gitlab-ci.yml contains "pytest"
  </acceptance_criteria>
  <done>GitLab CI and GitHub Actions configurations written</done>
</task>

<task type="auto">
  <name>Task 4: Implement pytest test suite verifying all Phase 1 components</name>
  <files>backend/tests/test_config.py, backend/tests/test_llm.py, backend/tests/test_events.py, backend/tests/test_audit.py, backend/tests/test_pipeline.py, backend/tests/test_workers.py, backend/tests/test_snapshots.py, backend/tests/test_cicd.py</files>
  <read_first>backend/app/providers/llm.py, backend/app/events/store.py, backend/app/workers/tasks.py</read_first>
  <action>Create a test suite in `backend/tests/`:
- `test_config.py`: tests settings and flags load correctly.
- `test_llm.py`: tests MockLLMProvider and FallbackLLMRouter.
- `test_events.py`: tests immutable write-only EventStore.
- `test_audit.py`: tests SQLAlchemy Audit record updates blocker.
- `test_pipeline.py`: tests InvestigationPipeline run status flow.
- `test_workers.py`: tests Celery run enqueuing.
- `test_snapshots.py`: tests SnapshotManager output structure and IncidentReportV1 model.
- `test_cicd.py`: tests yaml configuration validity.</action>
  <verify>pytest</verify>
  <acceptance_criteria>
    - pytest runs and exits successfully.
  </acceptance_criteria>
  <done>pytest suite complete and verified</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] docker-compose config passes validation.
- [ ] pytest runs all test scripts successfully.
</verification>

<success_criteria>
- All tasks completed
- Multi-container deploy parses successfully and all pytest unit tests for configurations, routing, event stores, and serializations pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-sprint-1/01-03-SUMMARY.md`
</output>
