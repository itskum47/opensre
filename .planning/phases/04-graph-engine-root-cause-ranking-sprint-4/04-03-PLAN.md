---
phase: 04-graph-engine-root-cause-ranking-sprint-4
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - backend/tests/test_benchmarks.py
autonomous: true
must_haves:
  truths:
    - "Synthetic benchmarks verify redis_saturation and database_outage incident scenarios with correct root cause ranking"
  artifacts:
    - path: "backend/tests/test_benchmarks.py"
      provides: "Benchmark test cases for OpenSRE incident reasoning engine"
      contains: "def test_benchmark_redis_saturation"
---

# Phase 4 Plan 03: Run Synthetic Incident Testing and Verification Benchmarks

## Objective
- **What:** Create incident scenario test cases and run benchmarks evaluating the accuracy of root cause hypotheses.
- **Why:** Proves that the evidence-based reasoning engine correctly identifies actual root causes (Redis saturation, DB outages) based purely on correlated timeline events.
- **Output:** test_benchmarks.py test file and final phase validation sign-off.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-graph-engine-root-cause-ranking-sprint-4/04-CONTEXT.md
@.planning/phases/04-graph-engine-root-cause-ranking-sprint-4/04-02-SUMMARY.md

## Tasks

<task type="auto">
  <name>Task 1: Implement synthetic incident benchmarks</name>
  <files>backend/tests/test_benchmarks.py</files>
  <read_first>backend/app/domain/incidents/pipeline.py, backend/app/domain/incidents/ranker.py</read_first>
  <action>Create `backend/tests/test_benchmarks.py` containing:
- `test_benchmark_redis_saturation`: Populates the EventStore with high Redis CPU usage metric points + Loki logs showing redis memory warnings + pod readiness failure events, runs the pipeline, and asserts that the Redis-related hypothesis is ranked #1.
- `test_benchmark_database_outage`: Populates the EventStore with a GitHub commit modifying DB schema + Loki logs showing postgres pool exhaustion + CoreV1 API Pod CrashLoopBackOff events, and asserts that the database/deployment hypothesis is ranked #1.</action>
  <verify>PYTHONPATH=. .venv/bin/pytest backend/tests/test_benchmarks.py</verify>
  <acceptance_criteria>
    - backend/tests/test_benchmarks.py contains both test functions
    - Both test cases pass successfully
  </acceptance_criteria>
  <done>Synthetic incident benchmarks implemented and verified</done>
</task>

## Verification
- Run `PYTHONPATH=. .venv/bin/pytest backend/tests/test_benchmarks.py` to ensure benchmark tests are fully green.

## Success Criteria
- [ ] Redis saturation scenario ranks the Redis hypothesis as #1.
- [ ] Database outage scenario ranks the DB/deployment hypothesis as #1.
