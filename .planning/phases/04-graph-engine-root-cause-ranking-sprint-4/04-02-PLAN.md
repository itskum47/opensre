---
phase: 04-graph-engine-root-cause-ranking-sprint-4
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - backend/app/domain/incidents/cli.py
  - backend/app/domain/incidents/pipeline.py
  - backend/tests/test_cli.py
autonomous: true
must_haves:
  truths:
    - "CLI commands investigate and explain complete execution correctly"
    - "InvestigationPipeline report stage produces a structured IncidentReportV1 payload"
  artifacts:
    - path: "backend/app/domain/incidents/cli.py"
      provides: "CLI entrypoints for investigate and explain"
      contains: "def investigate"
    - path: "backend/tests/test_cli.py"
      provides: "Unit and integration tests for CLI interface"
      contains: "def test_cli_commands"
---

# Phase 4 Plan 02: Implement CLI Commands and SDK Interfaces

## Objective
- **What:** Add the command-line interface `opensre` and wire the final `report` stage of `InvestigationPipeline`.
- **Why:** Exposes the incident reasoning engine functionality to SREs via the terminal, allowing them to initiate investigations and generate detailed root cause narratives.
- **Output:** cli.py CLI, updated pipeline.py report stage, and test_cli.py test suite.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-graph-engine-root-cause-ranking-sprint-4/04-CONTEXT.md
@.planning/phases/04-graph-engine-root-cause-ranking-sprint-4/04-01-SUMMARY.md

## Tasks

<task type="auto">
  <name>Task 1: Complete InvestigationPipeline report stage</name>
  <files>backend/app/domain/incidents/pipeline.py</files>
  <read_first>backend/app/domain/incidents/pipeline.py, backend/app/domain/incidents/schemas.py</read_first>
  <action>Implement the `report` method in `backend/app/domain/incidents/pipeline.py`.
It must read the ranked hypotheses, build a final Pydantic `IncidentReportV1` structure containing the timeline, graph summary, top candidates, and audit trail, and persist it to `report.json` under the investigation folder.</action>
  <verify>python -c "from backend.app.domain.incidents.pipeline import InvestigationPipeline; print(InvestigationPipeline.report)" exits 0</verify>
  <acceptance_criteria>
    - backend/app/domain/incidents/pipeline.py contains logic in `report` compiling and writing `report.json`
  </acceptance_criteria>
  <done>InvestigationPipeline report stage completed</done>
</task>

<task type="auto">
  <name>Task 2: Build opensre CLI commands</name>
  <files>backend/app/domain/incidents/cli.py</files>
  <read_first>backend/app/domain/incidents/pipeline.py, backend/app/providers/llm.py</read_first>
  <action>Create `backend/app/domain/incidents/cli.py` implementing a Click or argparse CLI defining commands:
- `opensre investigate <investigation_id>`: Instantiates the pipeline, executes it, and prints the top hypotheses with confidence scores.
- `opensre explain <investigation_id>`: Reads `report.json`, queries the `LLMProvider` Fallback router to generate a rich root-cause narrative, and outputs it.
Also configure a setup entrypoint in pyproject.toml / setup.py or an executable wrapper `bin/opensre` in the root.</action>
  <verify>python -c "from backend.app.domain.incidents.cli import investigate, explain; print(investigate, explain)" exits 0</verify>
  <acceptance_criteria>
    - backend/app/domain/incidents/cli.py contains functions `investigate` and `explain`
  </acceptance_criteria>
  <done>CLI commands investigated and implemented</done>
</task>

<task type="auto">
  <name>Task 3: Implement unit and integration tests for CLI</name>
  <files>backend/tests/test_cli.py</files>
  <read_first>backend/app/domain/incidents/cli.py</read_first>
  <action>Create `backend/tests/test_cli.py` to test that invoking CLI commands investigate and explain yields correct exits and prints output details.</action>
  <verify>PYTHONPATH=. .venv/bin/pytest backend/tests/test_cli.py</verify>
  <acceptance_criteria>
    - backend/tests/test_cli.py passes successfully
  </acceptance_criteria>
  <done>CLI test suite completed and verified</done>
</task>

## Verification
- Run `PYTHONPATH=. .venv/bin/pytest backend/tests/test_cli.py` to verify the command execution.

## Success Criteria
- [ ] Investigate command executes the pipeline successfully.
- [ ] Explain command generates persona-based explanation narratives.
