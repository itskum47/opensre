---
phase: 04-graph-engine-root-cause-ranking-sprint-4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/domain/incidents/graph.py
  - backend/app/domain/incidents/ranker.py
  - backend/app/domain/incidents/pipeline.py
  - backend/tests/test_graph_ranker.py
autonomous: true
must_haves:
  truths:
    - "GraphProvider builds directed service dependency maps correctly using networkx"
    - "RootCauseRanker computes multi-factor hypothesis scoring correctly"
  artifacts:
    - path: "backend/app/domain/incidents/graph.py"
      provides: "GraphProvider using networkx"
      contains: "class GraphProvider"
    - path: "backend/app/domain/incidents/ranker.py"
      provides: "RootCauseRanker multi-factor scorer"
      contains: "class RootCauseRanker"
---

# Phase 4 Plan 01: Implement Graph Engine and Root Cause Ranker

## Objective
- **What:** Create NetworkX service topology modeling and multi-factor ranking modules.
- **Why:** Build the incident mapping and hypothesis prioritization core of the evidence-based reasoning engine.
- **Output:** graph.py, ranker.py, and updated pipeline.py.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-graph-engine-root-cause-ranking-sprint-4/04-CONTEXT.md

## Tasks

<task type="auto">
  <name>Task 1: Implement GraphProvider using networkx</name>
  <files>backend/app/domain/incidents/graph.py</files>
  <read_first>backend/app/domain/incidents/timeline.py</read_first>
  <action>Create `backend/app/domain/incidents/graph.py` containing `GraphProvider` using the `networkx` library to:
- Build a directed service dependency map.
- Add nodes for discovered services (e.g. auth-service, db-service, redis).
- Add edges for dependencies based on communication paths (e.g., auth-service -> db-service).</action>
  <verify>python -c "from backend.app.domain.incidents.graph import GraphProvider; print(GraphProvider)" exits 0</verify>
  <acceptance_criteria>
    - backend/app/domain/incidents/graph.py contains "class GraphProvider"
  </acceptance_criteria>
  <done>GraphProvider implemented successfully</done>
</task>

<task type="auto">
  <name>Task 2: Implement RootCauseRanker multi-factor ranking</name>
  <files>backend/app/domain/incidents/ranker.py</files>
  <read_first>backend/app/domain/incidents/timeline.py, backend/app/domain/incidents/graph.py</read_first>
  <action>Create `backend/app/domain/incidents/ranker.py` containing `RootCauseRanker` which:
- Evaluates correlated event groups.
- Scores them using a formula weighting: Temporal Proximity (proximity to start), Evidence Density (anomalies ratio), Source Reliability (types of events), and Change History (presence of git deployment commits).
- Outputs list of sorted hypothesis dictionaries with score details.</action>
  <verify>python -c "from backend.app.domain.incidents.ranker import RootCauseRanker; print(RootCauseRanker)" exits 0</verify>
  <acceptance_criteria>
    - backend/app/domain/incidents/ranker.py contains "class RootCauseRanker"
  </acceptance_criteria>
  <done>RootCauseRanker implemented successfully</done>
</task>

<task type="auto">
  <name>Task 3: Wire Graph and Ranker into InvestigationPipeline stages</name>
  <files>backend/app/domain/incidents/pipeline.py</files>
  <read_first>backend/app/domain/incidents/pipeline.py, backend/app/domain/incidents/graph.py, backend/app/domain/incidents/ranker.py</read_first>
  <action>Update `graph`, `hypothesize`, and `rank` methods in `backend/app/domain/incidents/pipeline.py` to:
- Invoke `GraphProvider` to build and save the service topology graph as JSON.
- Invoke `RootCauseRanker` to compute root cause candidates and score explanations, persisting `hypotheses.json` under the investigation folder.</action>
  <verify>python -c "from backend.app.domain.incidents.pipeline import InvestigationPipeline; print(InvestigationPipeline.rank)" exits 0</verify>
  <acceptance_criteria>
    - backend/app/domain/incidents/pipeline.py imports `GraphProvider` and `RootCauseRanker`
    - `graph`, `hypothesize`, and `rank` methods invoke graph and ranker classes, persisting outputs
  </acceptance_criteria>
  <done>InvestigationPipeline stages wired successfully</done>
</task>

<task type="auto">
  <name>Task 4: Write unit tests for GraphProvider and RootCauseRanker</name>
  <files>backend/tests/test_graph_ranker.py</files>
  <read_first>backend/app/domain/incidents/graph.py, backend/app/domain/incidents/ranker.py</read_first>
  <action>Create unit tests verifying graph construction, edge mapping, scoring accuracy, and ranking correctness.</action>
  <verify>PYTHONPATH=. .venv/bin/pytest backend/tests/test_graph_ranker.py</verify>
  <acceptance_criteria>
    - backend/tests/test_graph_ranker.py passes successfully
  </acceptance_criteria>
  <done>Graph and Ranker test suite verified</done>
</task>

## Verification
- Run `PYTHONPATH=. .venv/bin/pytest backend/tests/test_graph_ranker.py` to ensure all graph and ranker logic is verified.

## Success Criteria
- [ ] Graph topology represents actual services dependency.
- [ ] RootCauseRanker outputs prioritized hypotheses with explicit, explainable scores.
